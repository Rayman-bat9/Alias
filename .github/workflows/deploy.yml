name: Build and Deploy on gh-pages

on:
  push:
    branches:
      - main

jobs:
    deploy:
        runs-on: ubuntu-latest
        steps:
            - name: deploy
              run: npm run deploy
